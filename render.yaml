services:
  - type: worker
    name: temu-shein-bot
    runtime: python
    pythonVersion: 3.11
    buildCommand: "pip install --no-cache-dir -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: DISCORD_TOKEN
        sync: false